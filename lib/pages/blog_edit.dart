import 'package:flutter/material.dart';

class BlogEditScreen extends StatelessWidget {
  const BlogEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
